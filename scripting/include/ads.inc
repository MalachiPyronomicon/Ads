/**
 * The API File for Ads
 * Do not edit this file at all otherwise you could cause problems!
 * SVN ID: $Id$
**/

public SharedPlugin:__pl_ads = 
{
	name = "ads",
	file = "ads.smx",
#if defined REQUIRE_PLUGIN
	required = 1,
#else
	required = 0,
#endif
};
 
public __pl_ads_SetNTVOptional()
{
	MarkNativeAsOptional("Add_Ads_Message");
}

/**
 * Add an ad message to the system. You can use the stock (Format_Add_Ad) for more advance formating options. 
 *
 * @param Message			The message to add to the ad system
 * @return					True if the message was added; false if not
 */
native bool:Add_Ads_Message(const String:message[]);


/* Stock */

stock bool:Format_Add_Ad(const String:text[], any:...)
{
	new String:message[255];
	VFormat(message, sizeof(message), text, 2);
	if (Add_Ads_Message(message))
	{
		return true;
	} else {
		return false;
	}
}